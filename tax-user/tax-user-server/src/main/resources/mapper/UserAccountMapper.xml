<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ebuy.tax.user.implementation.user_account.mapper.UserAccountMapper">

    <!-- 通用查询结果列 -->
    <sql id="UserAll_column">
         user.id as id,
         user.user_id as userId,
         user.balance as balance,
         user.remak as remak,
         user.status as status,
         user.reseve1 as reseve1,
         user.reseve2 as reseve2,
         user.reseve3 as reseve3,
         user.create_time as createTime,
         user.update_time as updateTime
    </sql>


    <sql id="UserAll_where">
        <where>
            1=1
            <if test="id != null and id !=''">
              AND user.id = #{id}
            </if>

            <if test="userId != null and userId !=''">
                AND user.user_id = #{userId}
            </if>

            <if test="balance != null and balance !=''">
                AND user.balance = #{balance}
            </if>

            <if test="remak != null and remak !=''">
                AND user.remak = #{remak}
            </if>

            <if test="status != null and status !=''">
                AND user.status = #{status}
            </if>

            <if test="reseve1 != null and reseve1 !=''">
                AND user.reseve1 = #{reseve1}
            </if>

            <if test="reseve2 != null and reseve2 !=''">
                AND user.reseve2 = #{reseve2}
            </if>

            <if test="reseve3 != null and reseve3 !=''">
                AND user.reseve3 = #{reseve3}
            </if>

            <if test="createTime != null and createTime !=''">
                AND user.create_time = #{createTime}
            </if>

            <if test="updateTime != null and updateTime !=''">
                AND user.update_time = #{updateTime}
            </if>

        </where>
    </sql>

    <sql id="UserAll_table">
        FROM
		tax_user_account user_account
    </sql>

    <!-- 查全部 -->
    <select id="queryList" resultType="com.ebuy.tax.user.api.user_account.entity.UserAccount"
            parameterType="com.ebuy.tax.user.api.user_account.entity.UserAccount">
        SELECT
        <include refid="UserAll_column" />
        <include refid="UserAll_table" />
        <include refid="UserAll_where" />
    </select>

    <!-- 查数量 -->
    <select id="queryCount" resultType="java.lang.Integer"
            parameterType="com.ebuy.tax.user.api.user_account.entity.UserAccount">
        SELECT
        count(1)
        <include refid="UserAll_table" />
        <include refid="UserAll_where" />
    </select>

    <!-- 分页查询列表 -->
    <select id="queryListForPage" resultType="com.ebuy.tax.user.api.user_account.entity.UserAccount">
        select
        <include refid="UserAll_column" />
        <include refid="UserAll_table" />
        <if test="entity.id != null and entity.id !=''">
            AND user.id = #{entity.id}
        </if>
        <if test="entity.userId != null and entity.userId !=''">
            AND user.user_id = #{entity.userId}
        </if>
        <if test="entity.balance != null and entity.balance !=''">
            AND user.balance = #{entity.balance}
        </if>
        <if test="entity.remak != null and entity.remak !=''">
            AND user.remak = #{entity.remak}
        </if>
        <if test="entity.status != null and entity.status !=''">
            AND user.status = #{entity.status}
        </if>
        <if test="entity.reseve1 != null and entity.reseve1 !=''">
            AND user.reseve1 = #{entity.reseve1}
        </if>
        <if test="entity.reseve2 != null and entity.reseve2 !=''">
            AND user.reseve2 = #{entity.reseve2}
        </if>
        <if test="entity.reseve3 != null and entity.reseve3 !=''">
            AND user.reseve3 = #{entity.reseve3}
        </if>
        <if test="entity.createTime != null and entity.createTime !=''">
            AND user.create_time = #{entity.createTime}
        </if>
        <if test="entity.updateTime != null and entity.updateTime !=''">
            AND user.update_time = #{entity.updateTime}
        </if>
        order by CREATE_TIME desc
        limit #{start,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER};
    </select>

    <select id="queryEntityByUserAccountEntity" resultType="com.ebuy.tax.user.api.user_account.entity.UserAccount">
        SELECT
        <include refid="UserAll_column" />
        <include refid="UserAll_table" />
        <include refid="UserAll_where" />
    </select>
</mapper>
