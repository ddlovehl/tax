<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ebuy.tax.user.implementation.customer.mapper.CustomerMapper">

    <!-- 通用查询结果列 -->
    <sql id="UserAll_column">
         user.id as id,
         user.user_id as userId,
         user.company_id as companyId,
         user.allot_merchant_id as allotMerchantId,
         user.allot_kefu_user_id as allotKefuUserId,
         user.allot_bursar_user_id as allotBursarUserId,
         user.allot_merchant_status as allotMerchantStatus,
         user.allot_kefu_status as allotKefuStatus,
         user.allot_bursar_stauts as allotBursarStauts,
         user.customer_status as customerStatus,
         user.customer_source as customerSource,
         user.reserve1 as reserve1,
         user.reserve2 as reserve2,
         user.reserve3 as reserve3,
         user.remark as remark,
         user.create_time as createTime,
         user.update_time as updateTime
    </sql>


    <sql id="UserAll_where">
        <where>
            1=1
            <if test="id != null and id !=''">
              AND user.id = #{id}
            </if>

            <if test="userId != null and userId !=''">
                AND user.user_id = #{userId}
            </if>

            <if test="companyId != null and companyId !=''">
                AND user.company_id = #{companyId}
            </if>

            <if test="allotMerchantId != null and allotMerchantId !=''">
                AND user.allot_merchant_id = #{allotMerchantId}
            </if>

            <if test="allotKefuUserId != null and allotKefuUserId !=''">
                AND user.allot_kefu_user_id = #{allotKefuUserId}
            </if>

            <if test="allotBursarUserId != null and allotBursarUserId !=''">
                AND user.allot_bursar_user_id = #{allotBursarUserId}
            </if>

            <if test="allotMerchantStatus != null and allotMerchantStatus !=''">
                AND user.allot_merchant_status = #{allotMerchantStatus}
            </if>

            <if test="allotKefuStatus != null and allotKefuStatus !=''">
                AND user.allot_kefu_status = #{allotKefuStatus}
            </if>

            <if test="allotBursarStauts != null and allotBursarStauts !=''">
                AND user.allot_bursar_stauts = #{allotBursarStauts}
            </if>

            <if test="customerStatus != null and customerStatus !=''">
                AND user.customer_status = #{customerStatus}
            </if>

            <if test="customerSource != null and customerSource !=''">
                AND user.customer_source = #{customerSource}
            </if>

            <if test="reserve1 != null and reserve1 !=''">
                AND user.reserve1 = #{reserve1}
            </if>

            <if test="reserve2 != null and reserve2 !=''">
                AND user.reserve2 = #{reserve2}
            </if>

            <if test="reserve3 != null and reserve3 !=''">
                AND user.reserve3 = #{reserve3}
            </if>

            <if test="remark != null and remark !=''">
                AND user.remark = #{remark}
            </if>

            <if test="createTime != null and createTime !=''">
                AND user.create_time = #{createTime}
            </if>

            <if test="updateTime != null and updateTime !=''">
                AND user.update_time = #{updateTime}
            </if>

        </where>
    </sql>

    <sql id="UserAll_table">
        FROM
		tax_customer customer
    </sql>

    <!-- 查全部 -->
    <select id="queryList" resultType="com.ebuy.tax.user.api.customer.entity.Customer"
            parameterType="com.ebuy.tax.user.api.customer.entity.Customer">
        SELECT
        <include refid="UserAll_column" />
        <include refid="UserAll_table" />
        <include refid="UserAll_where" />
    </select>

    <!-- 查数量 -->
    <select id="queryCount" resultType="java.lang.Integer"
            parameterType="com.ebuy.tax.user.api.customer.entity.Customer">
        SELECT
        count(1)
        <include refid="UserAll_table" />
        <include refid="UserAll_where" />
    </select>

    <!-- 分页查询列表 -->
    <select id="queryListForPage" resultType="com.ebuy.tax.user.api.customer.entity.Customer">
        select
        <include refid="UserAll_column" />
        <include refid="UserAll_table" />
        <if test="entity.id != null and entity.id !=''">
            AND user.id = #{entity.id}
        </if>
        <if test="entity.userId != null and entity.userId !=''">
            AND user.user_id = #{entity.userId}
        </if>
        <if test="entity.companyId != null and entity.companyId !=''">
            AND user.company_id = #{entity.companyId}
        </if>
        <if test="entity.allotMerchantId != null and entity.allotMerchantId !=''">
            AND user.allot_merchant_id = #{entity.allotMerchantId}
        </if>
        <if test="entity.allotKefuUserId != null and entity.allotKefuUserId !=''">
            AND user.allot_kefu_user_id = #{entity.allotKefuUserId}
        </if>
        <if test="entity.allotBursarUserId != null and entity.allotBursarUserId !=''">
            AND user.allot_bursar_user_id = #{entity.allotBursarUserId}
        </if>
        <if test="entity.allotMerchantStatus != null and entity.allotMerchantStatus !=''">
            AND user.allot_merchant_status = #{entity.allotMerchantStatus}
        </if>
        <if test="entity.allotKefuStatus != null and entity.allotKefuStatus !=''">
            AND user.allot_kefu_status = #{entity.allotKefuStatus}
        </if>
        <if test="entity.allotBursarStauts != null and entity.allotBursarStauts !=''">
            AND user.allot_bursar_stauts = #{entity.allotBursarStauts}
        </if>
        <if test="entity.customerStatus != null and entity.customerStatus !=''">
            AND user.customer_status = #{entity.customerStatus}
        </if>
        <if test="entity.customerSource != null and entity.customerSource !=''">
            AND user.customer_source = #{entity.customerSource}
        </if>
        <if test="entity.reserve1 != null and entity.reserve1 !=''">
            AND user.reserve1 = #{entity.reserve1}
        </if>
        <if test="entity.reserve2 != null and entity.reserve2 !=''">
            AND user.reserve2 = #{entity.reserve2}
        </if>
        <if test="entity.reserve3 != null and entity.reserve3 !=''">
            AND user.reserve3 = #{entity.reserve3}
        </if>
        <if test="entity.remark != null and entity.remark !=''">
            AND user.remark = #{entity.remark}
        </if>
        <if test="entity.createTime != null and entity.createTime !=''">
            AND user.create_time = #{entity.createTime}
        </if>
        <if test="entity.updateTime != null and entity.updateTime !=''">
            AND user.update_time = #{entity.updateTime}
        </if>
        order by CREATE_TIME desc
        limit #{start,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER};
    </select>

    <select id="queryEntityByCustomerEntity" resultType="com.ebuy.tax.user.api.customer.entity.Customer">
        SELECT
        <include refid="UserAll_column" />
        <include refid="UserAll_table" />
        <include refid="UserAll_where" />
    </select>
</mapper>
