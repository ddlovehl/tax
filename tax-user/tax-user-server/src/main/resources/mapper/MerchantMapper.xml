<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ebuy.tax.user.implementation.merchant.mapper.MerchantMapper">

    <!-- 通用查询结果列 -->
    <sql id="UserAll_column">
         user.id as id,
         user.user_id as userId,
         user.company_id as companyId,
         user.service_type as serviceType,
         user.merchant_source as merchantSource,
         user.merchant_status as merchantStatus,
         user.allot_user_id as allotUserId,
         user.allot_user_name as allotUserName,
         user.receive_type as receiveType,
         user.receive_clause as receiveClause,
         user.audit_status as auditStatus,
         user.remark as remark,
         user.reseve1 as reseve1,
         user.reseve2 as reseve2,
         user.reseve3 as reseve3,
         user.create_time as createTime,
         user.update_time as updateTime
    </sql>


    <sql id="UserAll_where">
        <where>
            1=1
            <if test="id != null and id !=''">
              AND user.id = #{id}
            </if>

            <if test="userId != null and userId !=''">
                AND user.user_id = #{userId}
            </if>

            <if test="companyId != null and companyId !=''">
                AND user.company_id = #{companyId}
            </if>

            <if test="serviceType != null and serviceType !=''">
                AND user.service_type = #{serviceType}
            </if>

            <if test="merchantSource != null and merchantSource !=''">
                AND user.merchant_source = #{merchantSource}
            </if>

            <if test="merchantStatus != null and merchantStatus !=''">
                AND user.merchant_status = #{merchantStatus}
            </if>

            <if test="allotUserId != null and allotUserId !=''">
                AND user.allot_user_id = #{allotUserId}
            </if>

            <if test="allotUserName != null and allotUserName !=''">
                AND user.allot_user_name = #{allotUserName}
            </if>

            <if test="receiveType != null and receiveType !=''">
                AND user.receive_type = #{receiveType}
            </if>

            <if test="receiveClause != null and receiveClause !=''">
                AND user.receive_clause = #{receiveClause}
            </if>

            <if test="auditStatus != null and auditStatus !=''">
                AND user.audit_status = #{auditStatus}
            </if>

            <if test="remark != null and remark !=''">
                AND user.remark = #{remark}
            </if>

            <if test="reseve1 != null and reseve1 !=''">
                AND user.reseve1 = #{reseve1}
            </if>

            <if test="reseve2 != null and reseve2 !=''">
                AND user.reseve2 = #{reseve2}
            </if>

            <if test="reseve3 != null and reseve3 !=''">
                AND user.reseve3 = #{reseve3}
            </if>

            <if test="createTime != null and createTime !=''">
                AND user.create_time = #{createTime}
            </if>

            <if test="updateTime != null and updateTime !=''">
                AND user.update_time = #{updateTime}
            </if>

        </where>
    </sql>

    <sql id="UserAll_table">
        FROM
		tax_merchant merchant
    </sql>

    <!-- 查全部 -->
    <select id="queryList" resultType="com.ebuy.tax.user.api.merchant.entity.Merchant"
            parameterType="com.ebuy.tax.user.api.merchant.entity.Merchant">
        SELECT
        <include refid="UserAll_column" />
        <include refid="UserAll_table" />
        <include refid="UserAll_where" />
    </select>

    <!-- 查数量 -->
    <select id="queryCount" resultType="java.lang.Integer"
            parameterType="com.ebuy.tax.user.api.merchant.entity.Merchant">
        SELECT
        count(1)
        <include refid="UserAll_table" />
        <include refid="UserAll_where" />
    </select>

    <!-- 分页查询列表 -->
    <select id="queryListForPage" resultType="com.ebuy.tax.user.api.merchant.entity.Merchant">
        select
        <include refid="UserAll_column" />
        <include refid="UserAll_table" />
        <if test="entity.id != null and entity.id !=''">
            AND user.id = #{entity.id}
        </if>
        <if test="entity.userId != null and entity.userId !=''">
            AND user.user_id = #{entity.userId}
        </if>
        <if test="entity.companyId != null and entity.companyId !=''">
            AND user.company_id = #{entity.companyId}
        </if>
        <if test="entity.serviceType != null and entity.serviceType !=''">
            AND user.service_type = #{entity.serviceType}
        </if>
        <if test="entity.merchantSource != null and entity.merchantSource !=''">
            AND user.merchant_source = #{entity.merchantSource}
        </if>
        <if test="entity.merchantStatus != null and entity.merchantStatus !=''">
            AND user.merchant_status = #{entity.merchantStatus}
        </if>
        <if test="entity.allotUserId != null and entity.allotUserId !=''">
            AND user.allot_user_id = #{entity.allotUserId}
        </if>
        <if test="entity.allotUserName != null and entity.allotUserName !=''">
            AND user.allot_user_name = #{entity.allotUserName}
        </if>
        <if test="entity.receiveType != null and entity.receiveType !=''">
            AND user.receive_type = #{entity.receiveType}
        </if>
        <if test="entity.receiveClause != null and entity.receiveClause !=''">
            AND user.receive_clause = #{entity.receiveClause}
        </if>
        <if test="entity.auditStatus != null and entity.auditStatus !=''">
            AND user.audit_status = #{entity.auditStatus}
        </if>
        <if test="entity.remark != null and entity.remark !=''">
            AND user.remark = #{entity.remark}
        </if>
        <if test="entity.reseve1 != null and entity.reseve1 !=''">
            AND user.reseve1 = #{entity.reseve1}
        </if>
        <if test="entity.reseve2 != null and entity.reseve2 !=''">
            AND user.reseve2 = #{entity.reseve2}
        </if>
        <if test="entity.reseve3 != null and entity.reseve3 !=''">
            AND user.reseve3 = #{entity.reseve3}
        </if>
        <if test="entity.createTime != null and entity.createTime !=''">
            AND user.create_time = #{entity.createTime}
        </if>
        <if test="entity.updateTime != null and entity.updateTime !=''">
            AND user.update_time = #{entity.updateTime}
        </if>
        order by CREATE_TIME desc
        limit #{start,jdbcType=INTEGER},#{pageSize,jdbcType=INTEGER};
    </select>

    <select id="queryEntityByMerchantEntity" resultType="com.ebuy.tax.user.api.merchant.entity.Merchant">
        SELECT
        <include refid="UserAll_column" />
        <include refid="UserAll_table" />
        <include refid="UserAll_where" />
    </select>
</mapper>
